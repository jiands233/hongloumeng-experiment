# 红楼梦残稿重建实验：冻结语料规则

用户当前授权止于 Phase 1.5。不得自行启动 Phase 2、命运推演、后四十回规划或正文续写；后续每阶段服从用户的明确指令。

## 默认入口与证据层

1. 开始文学工作前，运行 `python3 scripts/verify_canonical.py`。校验失败必须停止对该版本的文学推理，不得静默重建或修改哈希。
2. 默认只通过 `python3 scripts/read_canonical.py 章号` 读取 `canonical/chapters/001.md` 至 `080.md`，同时使用 manifest 与 known_issues 中的资格限制。禁止对整个工作区做正文全文检索；检索仅限 `canonical/chapters/`，默认遵守 `.ignore`。
3. 原始目录、zip、`excluded_sources/`、`research/phase1/` 是留存与审计材料，后续文学推理默认不读取。Phase 1 旧报告不替代已修复底本，尤其不得据旧报告复活本轮剥离的第50回段落、第64回扩补或第78回待定叙事。
4. 脂批仅在任务需要时通过 `--layer zhiyan` 单独读取。只称“本地标作某本的批语”，不得把所有注释都归于脂砚斋个人。正文证据与脂批证据分栏引用；带批来源的真实性没有经手稿认证。
5. `annotations/undetermined/` 和 provenance 中 `allowed_for_inference=false` 的原文仅用于数据核查，不用作情节、性格、年代或终局证据。缺文标记、疑缺、缺字与补配标记是实验编辑标记，不是小说文字。补配与异文敏感处只能作为有条件证据，关键推论应说明是否依赖这些段落。
6. `canonical/metadata/provenance.json` 用于按 P/N/A/E/G 编号定位来源与处理记录，不作为一部可以顺读的正文。隔离材料仅可读取其路径、哈希、原因等元数据，不得顺着引用打开正文。

## 禁止的剧情依据

禁止主动访问或利用：程甲本81—120回、程乙本81—120回、高鹗续书及情节梗概、其他古今续书、现代续书、影视改编剧情、现代红学家对佚失后四十回的具体重建方案；亦禁止本地第67回程甲附录、第64回被剥离的程甲改句／后人扩补、第41回隔离的现代校释。不得联网找结局或用预训练记忆补缺。若未来加入新材料，先隔离并更新来源清单，不得混入 v1。

## 冻结与写入边界

原始 source 与 zip 永不改动。本次 `canonical/` 是 v1 冻结快照，不是原作者定本；不得在后续推理或续写中顺手校字。只有用户另行授权修订底本时，才建立新版本目录并写变更记录，保留 v1、哈希与 Git 历史。生成器在发现冻结文件时拒绝覆盖。

这些规则与显式读取器降低误读风险，并非操作系统访问控制，也不能物理抹除模型训练记忆。数据完整性审计可对排除文件作不解码的字节哈希检查，不能将其内容提交给文学推理上下文。
